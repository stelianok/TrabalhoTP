import java.awt.*;
import javax.swing.*;

public class UpdateProductWindow extends JPanel {
  UpdateProductWindow() {
    setSize(800, 800);
    setLayout(new BorderLayout());

    setVisible(true);
  }
}
