import java.awt.*;
import javax.swing.*;

public class ListAllProductsWindow extends JPanel {
  ListAllProductsWindow() {
    setSize(800, 800);
    setLayout(new BorderLayout());

    setVisible(true);
  }
}
