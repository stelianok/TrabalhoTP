import java.awt.*;
import javax.swing.*;

public class ListAllSuppliersWindow extends JPanel {
  ListAllSuppliersWindow() {
    setSize(800, 800);
    setLayout(new BorderLayout());

    setVisible(true);
  }
}
